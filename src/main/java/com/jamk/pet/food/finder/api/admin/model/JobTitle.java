package com.jamk.pet.food.finder.api.admin.model;

public enum JobTitle {

    // In production, the full list of jobs is uploaded once during the initial application setup
    CHIEF_OPERATOR, OPERATOR, CEO, SYSTEM_ADMINISTRATOR, SALES_MANAGER, MARKETING_MANAGER;
}
